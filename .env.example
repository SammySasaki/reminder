# Client-safe (prefix with VITE_ so Vite exposes them to the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — never expose to the client bundle
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
REDIS_URL=
TIMEZONE=America/Los_Angeles
FALLBACK_FAMILY_MEMBER=Sarah
DEFAULT_STT_LANGUAGE=ko
